"""
CLI entry point for Chronicon (future enhancement).

This is a placeholder for a future CLI tool. For now, use the Python API directly.

Potential commands:
  chronicon run <workflow.py>::<workflow_name> --args '{"x": 1}'
  chronicon replay <execution_id>
  chronicon diff <exec1> <exec2>
  chronicon list
  chronicon inspect <execution_id>
"""

def main():
    print("Chronicon CLI - coming soon!")
    print("For now, use the Python API:")
    print()
    print("  from chronicon import workflow, execute, replay")
    print("  # ... your code here")


if __name__ == "__main__":
    main()
